{
  "name": "xtten-desktop-agent",
  "version": "0.1.0",
  "description": "XTTEN Agent for employee attendance, leave, and work reporting",
  "main": "main.js",
  "private": true,
  "author": "XTTEN",
  "scripts": {
    "dev": "electron .",
    "generate:icon": "node scripts/generate-icon.js",
    "pack:win": "npm run generate:icon && electron-builder --win nsis msi --x64",
    "pack:dir": "npm run generate:icon && electron-builder --dir"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.xtten.agent",
    "productName": "XTTEN Agent",
    "executableName": "xtten-desktop",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "config.json",
      "renderer/**/*"
    ],
    "extraResources": [
      {
        "from": "build/app.ico",
        "to": "icons/app.ico"
      },
      {
        "from": "../activity-agent.js",
        "to": "agent/activity-agent.js"
      },
      {
        "from": "../windows-telemetry.ps1",
        "to": "agent/windows-telemetry.ps1"
      }
    ],
    "win": {
      "icon": "build/app.ico",
      "artifactName": "xtten-agent-setup.${ext}",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "msi",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  },
  "allowScripts": {
    "electron@33.4.11": true,
    "electron-winstaller@5.4.0": true
  },
  "dependencies": {
    "png-to-ico": "^2.1.8",
    "sharp": "^0.35.2"
  }
}
